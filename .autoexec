#!/bin/bash
files=(
    env
    functions
    ssh_shell_aliases
    shell_aliases
    extra_paths
    nvm
    gvm
    rvm
    dvm
    #pyenv
    adb
    togetair/.shell_aliases
    urbantz/.shell_aliases
    urbantz/.env
    bsit/.env
    bsit/.shell_aliases
)

for file in "${files[@]}"
do
    source ~/.dotfiles/.$file;
done
