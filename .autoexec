#!/bin/bash
files=(
    env
    functions
    ssh_shell_aliases
    shell_aliases
    extra_paths
    nvm
    gvm
    rvm
    dvm
    adb
    urbantz/.shell_aliases
    urbantz/.env
)

for file in "${files[@]}"
do
    source ~/.dotfiles/.$file;
done
